

CREATE TRIGGER WordLength
    ON dbo.Word
    AFTER INSERT, UPDATE
    AS BEGIN
             SET Length = VALUE.length WHERE WordID = new.WordID;
END




CREATE PROCEDURE dbo.spWordMap
    @Line INT,
    @Paragrath INT  = ''

insert into dbo.word (wordID,Value,Length)
VALUES (@WordID,@Value, null);
AS
BEGIN
        SELECT
END

